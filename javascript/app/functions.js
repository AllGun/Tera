//Uma função que calcula a área de um retângulo

function areaRetangulo(lado1, lado2) {
    
    return lado1 * lado2;

}

// Eu posso calcular a área com a função e armazenar em uma variável
const retanguloA = areaRetangulo(10,10);

// tb é possível impripir direto da função
console.log(retanguloA); 
console.log(areaRetangulo(3,4));